# 买卖条件文档

## 卖出条件 (`judge_sell_id`)

### `sell_id = 1`：标准卖出策略 1

此 ID 包含一套基于多时间周期和技术指标的综合卖出策略，按以下优先级顺序执行：

1.  **清仓信号**：
    - **条件**：周线出现 MACD 顶背离（价格创新高但 DIF 未创新高），并且 120分钟 K 线图上出现 MACD 死叉（3天内）。
    - **操作**：建议清仓。

2.  **减仓信号**：
    - **条件**：日线出现 MACD 顶背离（价格创新高但 DIF 未创新高），并且 120分钟 K 线图上出现 MACD 死叉（3天内）。
    - **操作**：建议卖出 1/3 仓位。

3.  **阶梯式减仓信号**：
    - **触发条件**：日线 SAR 指标被跌破（3天内）。
    - **操作**：根据周线 RSI 指标的强度执行不同比例的卖出：
        - **周线 RSI > 90**：建议卖出全部剩余仓位。
        - **周线 RSI > 85**：建议卖出剩余仓位的 1/2。
        - **周线 RSI > 80**：建议卖出 1/3 仓位。

### `sell_id = 2`：标准卖出策略 2

此 ID 包含一套基于多时间周期和技术指标的综合卖出策略，按以下优先级顺序执行：

1.  **清仓信号**：
    - **条件**：周线出现 MACD 顶背离（价格创新高但 DIF 未创新高），并且 日线 K 线图上出现 MACD 死叉（3天内）。
    - **操作**：建议清仓。

2.  **减仓信号**：
    - **条件**：日线出现 MACD 顶背离（价格创新高但 DIF 未创新高），并且 日线 K 线图上出现 MACD 死叉（3天内）。
    - **操作**：建议卖出 1/3 仓位。

3.  **阶梯式减仓信号**：
    - **触发条件**：日线 SAR 指标被跌破（3天内）。
    - **操作**：根据周线 RSI 指标的强度执行不同比例的卖出：
        - **周线 RSI > 90**：建议卖出全部剩余仓位。
        - **周线 RSI > 85**：建议卖出剩余仓位的 1/2。
        - **周线 RSI > 80**：建议卖出 1/3 仓位。

---

## 买入条件 (`judge_buy_id` / `judge_t_id`)

系统支持不同的买入策略，通过 `judge_buy_id`（新开仓）或 `judge_t_id`（做 T 买回）进行选择。两者复用相同的逻辑 ID，但提示信息会有所区别：
- **`judge_buy_id`**：提示 **“新资金买入信号”**。
- **`judge_t_id`**：提示 **“做 T 买回信号”**（用于买回之前卖出的资金）。

### `buy_id` = 1：保守型买入
- **条件**：日线 RSI < 20 **并且** 周线 RSI < 25。

### `buy_id` = 2：标准型买入 / 做 T 买回
- **说明**：此 ID 常用于判断卖出后的资金买回时机。
- **条件**：日线 RSI < 25 **并且** 周线 RSI < 30。

### `buy_id` = 3：指数保护型买入
- **说明**：此 ID 用于监控市场大盘是否处于超卖状态。
- **条件**：**上证指数 (000001)** 或 **创业板指数 (399006)** 的日线 RSI < 25。

### `buy_id` = 4：极度保守型买入
- **条件**：日线 RSI < 20 **并且** 周线 RSI < 20。
