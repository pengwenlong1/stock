def find_local_maxima(prices, distance=10, prominence=None):
    prices = np.array(prices)
    peaks_idx, properties = find_peaks(
        prices,
        distance=distance,
        prominence=prominence  
    )
    peaks_val = prices[peaks_idx]
    return peaks_idx, peaks_val

distance 取 10
atr = talib.ATR(high, low, close, timeperiod=14)
prominence = 0.8 * atr[-1]  # 取最新ATR值



局部高点测试：
专门一个脚本去实现局部高点的判断，并且将所有日线局部高点和周线局部高点画图展示出来