## RSI 相关指标判断逻辑

### RSI flag 状态跟踪

**初始化**：`rsi_flag = 0`

**状态更新规则**：
1. 周线 RSI > 80 → `rsi_flag = 1`（一级警戒）
2. 周线 RSI > 85 → `rsi_flag = 2`（二级警戒）
3. 周线 RSI > 90 → `rsi_flag = 3`（三级警戒/清仓信号）
4. 当 5 日均线跌破 10 日均线时 → `rsi_flag = 0`（重置）

**说明**：
- RSI flag 反映的是从上次均线跌破后到当前的周线 RSI 峰值等级
- 均线跌破后，flag 重置为 0，并重新根据新的 RSI 峰值更新


RSI(6)